# Игры — механики из референсных видео

Оба референса — «fake ad» гиперказуальные tower defense. Делаем свои версии: та же петля,
свои ассеты (без чужих персонажей/брендов).

## 1. Merge Defense (референс: LUDUS, IMG_1777.MP4, 23 с)

**Экран.** Вид сверху. Сетка 5×4 зелёных плиток (поле для башен). Вдоль поля Г-образная
дорога; враги входят справа из портала, идут по дороге; база — в конце дороги.

**Цикл.**
1. На плитке появляются 2–3 иконки выбора (🔥 fire, ⚙ robot, 🌪 storm…) — тап ставит башню.
2. Башня стреляет по врагам в радиусе автоматически.
3. **Слияние:** перетащить башню на другую — если пара есть в таблице комбо, рождается
   новая башня: `FIRE + ROBOT → FIREBOT`, `STORM + FIREBOT → MECHAGOD`. Слияние = прыжок силы.
4. Волны: мелкие скелеты → большой босс-червь (много HP, длинное тело).
5. Между волнами: выбор стихии/бонуса из 4–5 иконок (fire, dark, nature, lightning, ice).
6. Конец уровня → «Choose the next level» → следующий уровень (другая раскладка дороги).

**Сущности.** Tower{type, level, dmg, range, rate}, Enemy{hp, speed, pathPos}, Wave[],
MergeTable: Map<"A+B", C>. Ввод: tap (поставить), drag-and-drop (слить).

## 2. Snake Defense (референс: Evo Defense, IMG_1778.MP4, 33 с) — РЕАЛИЗОВАНО

Код: `src/games/snake-td/`. Движок чистый TS (`engine/`, 25 vitest-тестов), рендер canvas 2D
(`render/draw.ts`), React-обвязка `SnakeDefense.tsx` (drag-and-drop, оверлеи).

**Правила (наша версия).**
- Кольцевая дорога: портал (справа сверху) → по кольцу → база под порталом. Внутри 4×4 слота.
- Змея = голова (босс) + сегменты, у каждого своё HP (растёт к хвосту). Змея **не исчезает** у
  базы, а идёт на следующий круг; каждый проход лидера через базу = −1 жизнь (голова −3). 10 жизней.
- Волна закончена, когда змея убита целиком. 10 волн, HP растёт экспоненциально (`engine/snake.ts`).
- Магазин: 3 предложения, обновить за золото (дорожает). Бойцов тянут на слот; двух одинаковых
  одного уровня — друг на друга = уровень +1 (урон ×1.5). Перетащить бойца в магазин = продать.
- 5 типов: Вольт (быстрый, цепь 30%), Фрост (замедляет), Блейз (сплэш по соседям), Веном (яд),
  Шэдоу (дальний, мощный). Радиус растёт с уровнем.
- Уровень 3 → «Эволюция»: надёжная (урон ×1.8, 100%) или рискованная (×3, 50%; провал = −1 ур.).
- Ивенты перед чётными волнами: золотая лихорадка, бешеная змея, подарок, заморозки.
- Рекорд = очки (сегменты + 100×волна), пишется в CloudStorage через `onScore`.

**Баланс.** Проверяется авто-игроком (dev-хук `window.__std`, см. `SnakeDefense.tsx`): жадный
бот с идеальным слиянием выигрывает 10 волн с 4–7 жизнями, теряя их с 7-й волны.

**Идеи дальше.** Уровни/карты с другой формой дороги; боссы с абилками; ежедневные ивенты;
рискованные эволюции с уникальными спрайтами; лидерборд по Telegram-друзьям.

## Общее

- Оба — портрет, один палец, сессии 1–3 мин → идеально для Telegram Mini App.
- Рендер: canvas 2D (60 fps на телефоне, без тяжёлых движков). Ассеты — свои спрайты/эмодзи.
- Рекорды через `lib/storage.ts` (CloudStorage). Реестр — `src/games/registry.ts`.
- Snake Defense готова (v1). Следующая — Merge Defense.
